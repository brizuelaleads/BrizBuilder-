/** Static, non-interactive atmosphere; colors are inherited from the shell. */
export function ShellBackground() {
  return <div className="crm-shell-background pointer-events-none" aria-hidden="true" />;
}
